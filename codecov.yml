codecov:
  branch: master

coverage:
  precision: 3
  round: nearest
  range: "50...100"

  status:
    project:
      default:
        # add a threshold
        threshold: null

comment:
  layout: "header, diff, changes, uncovered, tree"
  behavior: new